# Beyond the JetPack 6.2.1 image (torch 2.8 NVIDIA wheel, tensorrt 10.3, python 3.10 already present).
# pip3 install -r requirements-jetson.txt
numpy<2            # keep the torch/tensorrt python bindings happy
opencv-python-headless>=4.8   # or use the JetPack-provided cv2 (skip this line if `import cv2` works)
onnx>=1.15
onnxruntime>=1.17  # CPU wheel is enough for verification; GPU wheel from NVIDIA jetson-zoo optional
transformers>=4.45,<5
sentencepiece
protobuf
accelerate
optimum[exporters]>=1.20      # Task 3 route A only
jetson-stats                  # optional jtop backend: sudo pip3 install jetson-stats && reboot
# --- Task 1 export/reference ONLY (run on an x86 host if mmcv is painful on aarch64) ---
# pip install -U openmim && mim install "mmengine>=0.10" "mmcv>=2.0.1" "mmpose>=1.3"
# onnxsim   # optional, for --simplify
